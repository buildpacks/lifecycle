[[processes]]
  type = "some-default-process-type"
  direct = true
  command = "/some/command"
  args = ["some", "command", "args"]
  buildpack-id = "buildpack1.id"
  default = true
[[processes]]
  type = "some-second-default-process-type"
  direct = true
  command = "/some/command"
  args = ["some", "command", "args"]
  buildpack-id = "buildpack2.id"
  default = true
[[processes]]
  type = "some-process-type"
  direct = true
  command = "/some/command"
  args = ["some", "command", "args"]
  buildpack-id = "buildpack3.id"

