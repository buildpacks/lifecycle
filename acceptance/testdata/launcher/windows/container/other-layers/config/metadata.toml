[[buildpacks]]
  id = "some-buildpack"
  api = "0.9"

[[processes]]
  type = "web"
  command = "echo"
  args = ["Executing", "other-layers", "web", "process-type"]
  direct = false
  buildpack-id = "some-buildpack"
